# One-page claim/evidence ledger

| Claim | Eligible artifact and exact field | Value | Category / permitted wording |
|---|---|---:|---|
| A globally shared key center cancels from attention logits; a globally shared value center is restored once. | Proposition 1 in `main.tex`, derived from softmax shift invariance and `P1=1`. | Exact in real arithmetic. | Mathematical identity. |
| Page/head scalar scales factor through completed QK and PV fragments. | Proposition 1: `q(sZ)^T=s(qZ^T)` and `P(sZ)=s(PZ)`. | Exact in real arithmetic. | Mathematical identity. |
| Mixed exact and quantized segments can be merged without elementwise historical-KV reconstruction. | Proposition 1 and online merge equations in `main.tex`; final raw block `attention_implementation.implementation`, `.logical_attention_segments`, `.value_center_restoration`. | `page_gauge_segmented_flashinfer_merge`; 2 segments; `attention_add`. | Mathematical identity plus implementation fact. |
| The algebraic execution matches explicit attention over the reconstructed mixed cache. | `results/page_gauge_int8/heterogeneous_fa2_correctness.json`, all `records[*].candidate_vs_reconstructed_fp16_fa2` and `split_invariance`. SHA `20c64fc...367d`. | 27/27 records and 18/18 split checks pass; max abs `6.1035e-5`; min cosine `0.999999821`; max LSE abs `9.8705e-5`. | Development diagnostic. Supports factorization, not final model fidelity or final-kernel speed. |
| The final fixed policy preserves predictive behavior relative to optimized FP16 FlashInfer. | `quality/aggregate.json`, `page_gauge_vs_flashinfer_fp16.point` and `.gate`. SHA `7adbbd...4f9`. | 9,216 B1 logit vectors; min cosine `0.9987073`; top-1 `0.9989149`; whole-window bootstrap top-1 lower95 `0.9986979`; PPL-ratio upper95 `1.0001982`; all gates pass. | Post-failure repair followed by post-adaptation fixed-policy confirmation. Do not call untouched held-out selection. |
| FP16 FlashInfer itself agrees with HF SDPA on the same cohort. | Same artifact, `flashinfer_fp16_vs_hf_sdpa_fp16.point` and `.gate`. | Min cosine `0.9995704`; top-1 `0.9991319`; all gates pass. | Final measured confirmation result. |
| Runtime-appended KV pages actually age into and are consumed from INT8 storage. | Same aggregate `protocol.runtime_generated_int8_pages`; each PG speed block `correctness.runtime_finalization_consumption`. | 48 teacher-forced decode-time pages consumed as INT8. | Final measured result / runtime invariant. |
| Final served KV storage is reduced honestly, including all exact regions and retained INT8 storage. | `final_performance_analysis.json`, `served_cache.*`; PG raw block `cache_build.selected_backend_cache_served_bytes_excluding_following_canary` and `prefix_storage_accounting`. SHA `42c355...19fc`. | B4 FP16 `11,542,724,608` B; PageGauge `7,288,520,704` B; 36.8562% reduction; 1.58369x KV-byte ratio. | Final deterministic accounting. Gross current layout; capacity claim is KV-cache-only at a fixed KV-memory budget. |
| PageGauge improves sustained complete-decoder latency over optimized FP16 FlashInfer. | Same performance artifact, `aggregates.cache_neutral.wall_ms`. | B4 17.2318 vs 15.2141 ms/step; 1.132624x; within-protocol hierarchical 95% CI `[1.132340,1.132797]`. | Final measured result. Eight fresh processes, four adjacent pairs, two fixture/seed clusters. |
| Timing is fair and independent of co-resident-cache artifacts. | Same artifact `protocol.*`, `inputs[*].performance_execution_gate_passed`, and `acceptance.*`. | ABBA then BAAB; 8 fresh processes; all same-backend execution gates pass. | Implementation/protocol fact. |
| Exact precision is a robustness allocation, not a novelty claim. | Final raw PG `configuration` and `cache_build`. | S4 prefix = 64 tokens; A128 fixed suffix = 2,048 tokens; T768 recent tail; 86.92% of represented logical history is quantized at endpoint. | Final implementation fact and interpretation. |
| Policy selection was adaptive. | `quality/aggregate.json`, `protocol.adaptive_policy_disclosure`; original selection manifest SHA `D1C867...798F` and failure record. | S4/A128/T768 was selected after diagnosis of an earlier TEST failure. | Post-failure repair. Must be disclosed; limits confirmatory scope. |
| Evidence scope is one system point. | Raw performance blocks `environment`, `configuration`, `claim_scope`. | Mistral-7B-v0.3; RTX 5090/SM120; B4; C20,480; D1,536; WSL2; complete decoder, not full serving stack. | Limitation. No cross-model/GPU generalization claim. |
