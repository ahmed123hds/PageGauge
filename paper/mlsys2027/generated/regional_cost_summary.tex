The clean four-policy pilot measures reference/A0 median ratios of 1.0149 (cache-neutral) and 1.0137 (cache-hot), with unchanged quantizer and kernels (Table~\ref{tab:regional-cost}). These are fixed-order, one-process-per-policy observations, not confidence intervals or a new FlashInfer speedup. Balanced fresh-process replication is required before promotion.
