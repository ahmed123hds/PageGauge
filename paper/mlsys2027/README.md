# MLSys 2027 development manuscript workspace

`main.tex` now assembles the main-paper development draft: introduction,
related work, original factorization contract, implementation/memory model,
supporting error analysis, controlled evaluation, limitations and references.
Build it with `bash paper/mlsys2027/build_main.sh`. The current output is seven
pages (references begin on page seven), eight tables and one TikZ figure.
It is **not submission-ready**: generated tasks, serving, shape/GPU coverage
and independently frozen final evaluation remain incomplete.

The main draft preserves both the original optimized FI/PG speed result and
the negative common-engine pilot, rather than mixing their execution paths.
`build_main_metrics.py` revalidates all eight original optimized worker
payloads and reproduces their hierarchical reduction. Its separate
`generated/main_evidence_manifest.json` also verifies the completed Qwen A128/A0
transfer evidence and independently checks the cache-byte formula. The main
draft includes that transfer result, the four-policy history160/exact32 cost
pilot, and the six-case Qwen own-greedy generation smoke. The new builders
verify raw timing/monitoring/accounting and generation evidence separately.
Balanced reference/A0 and FI/A0 replications are complete and included as a
separate paragraph. `build_replication_summary.py` revalidates sixteen blocks
and reproduces both bootstrap reductions. The FI/A0 CPU recovery and its two
FlashInfer auxiliary quality warnings are disclosed. Neither result is an
automatic policy promotion or full-serving result. The evidence-only PDF below currently
ends with the Mistral four-policy ablation.

The main draft uses nine primary-source reference entries, including the
published HPCA2026 BitDecoding citation. Full related-work coverage still needs
completion. The vendor style is unchanged. `xurl` resolves long-reference URL
overflow; content-side `hypertexnames=false` resolves duplicate destination
warnings, and links have no colored boxes. All seven pages were rendered and
inspected; the latest cost/generation update's pages5--7 were rechecked.
The final timing table uses a content-side float-placement hint so it shares
page6 with the preceding tables instead of occupying an isolated float page.

`development_results.tex` is a six-page evidence report, not the final main
paper. It preserves positive and negative development findings while E2--E5
continue. No independent final TEST result is claimed. The submitted
`paper/axiom2026` files remain unchanged.

Build from WSL with `bash paper/mlsys2027/build_development_results.sh`.
The builder verifies 666 completed evidence files and produces eleven LaTeX
tables plus `generated/evidence_manifest.json`. The PDF is typeset with
pdflatex, not generated by Python. The Python utility only reduces verified
result JSON into table artifacts. Run the CPU table checks with
`python -m unittest discover -s tests -p test_mlsys_evidence_tables.py`.

The official 2027 call links the unchanged MLSys2025 style archive:
https://mlsys.org/Conferences/2027/CallForResearchPapers
https://media.mlsys.org/Conferences/MLSYS2025/mlsys2025style.zip
The downloaded archive and extracted vendor files are retained under `vendor`.
Only this development document overrides the notice text to avoid falsely
claiming that an internal draft is already under review. The vendor style is
not edited. Final submission eligibility, references, author review, and
anonymization still need their separate audit.

QA: pdflatex twice; all six rendered pages visually inspected; no overfull boxes,
all embedded Type1 fonts. The historical template emits duplicate table-anchor
warnings with modern hyperref; visible references resolve, links have no boxes.
Resolve those before final submission if hyperlinks are retained. Current
report has no bibliography and must not be mistaken for the complete related
work or final paper.

The Qwen table now includes corrected-native Kitty-Pro on eight exactly matched
TRAIN windows, with its own HF reference and 83.16% KV reduction. The two int32
address-operand corrections are disclosed; these are not quantizer changes or
PageGauge changes. Native latency pilots are now included below, with their
interruption limitations. The report also preserves
PageGauge's short-context negative memory result.

The common-engine B4 timing table preserves a negative PageGauge/FlashInfer
point comparison and all three-repeat ranges. It is a single-workload pilot,
not a hierarchical CI or optimized-system result. CPU-backed cache preparation
removes retained GPU FP16 reference caches; peak allocation and served KV are
reported separately. The later fixed capacity grid is not a global boundary search.

Table 7 adds eight metadata-selected PG-19 TRAIN books for Mistral, with
12,288 recurrent labels and paired-book descriptive PPL intervals. This is
model-token PPL on selected development windows, not the official whole-corpus
word-normalized PG-19 score or independent TEST. All nine CPU table checks pass;
the changed fourth page was rendered and visually checked after compilation.

Table 8 reports the attention-only graph-dispatch pilot for both FI and PG,
including the small 1.01731 point ratio, overlapping repeat ranges, full
same-cache execution validation, and separate setup/memory costs. This is not
a confirmed speedup or a production-default promotion. The fourth page was
rendered and checked again after adding this section.

Table 9 retains the completed original B4/B8/B16 capacity grid, distinguishing
measured CUDA OOM from conservative analytical exclusion. Subsequent text
discloses the allocator and non-graph planner correction, full B8 validation,
and the fresh 25.68 ms/step point. This is not a replicated comparative speed
claim. All eight native default/expandable allocator checks are complete;
none rescues the four originally failed native cells. The supporting shift-invariant
error bound does not claim new-algorithm novelty or a full-network guarantee.
Changed pages 4 and 5 were rendered and checked after this update.

Previous ten-table PDF SHA256 (2026-09-09, superseded below):
`4BF8BA8AF8FAF91EED02809C507675D2DC74C6CB8F25AEB6687B4E6144E76230`.

Table 7 now includes the full eight-book Qwen PG19 TRAIN cohort (not only its
first pilot): PG/HF PPL ratio 1.00036665, descriptive interval
[0.99991387, 1.00101386], top-1 0.99633789, and 36.856% served KV reduction.
The same books are used for Mistral/Qwen with their own tokenizers; labels are
not identical across models. Both PG ratio intervals include one, without a
prospective non-inferiority claim. Pages 4/5 were rerendered and checked.

`related_work.tex` and seven complete-author, proceedings-verified entries in
`references.bib` are draft components for the full manuscript; they are not yet
included in this evidence PDF and are not exhaustive related-work coverage.

Table 10 adds native NSNQuant/Mistral and Kitty/Qwen request-cost pilots:
full-prefix/cache-packing cost, complete recurrent decoding repeat ranges,
served KV reduction, and prefill/decode peak allocation. Three original arms
completed; the fourth was externally interrupted and rerun after WSL restarted.
The interrupted directory remains unchanged and the recovery reducer verifies
the retained/replacement evidence. Kitty's time gap precludes an adjacent
matched speed claim. No cross-stack PageGauge ratio, CI, maximum throughput or
real-server result is claimed. New pages 5/6 were rendered and inspected.

Table 11 adds all four exact-region policies on eight Mistral PG19 TRAIN books.
Removing static suffix A128 reduces PageGauge KV by14.73% (46.16% versus FP16),
with top1/HF99.805% and PPL/reference0.999915. These are exploratory development
results, not a general quality-superiority or speed claim. No default promotion;
clean timing and cross-model confirmation are pending. All144 selected
same-cache head-group probes per policy passed. Ten CPUtablechecksPASS.
The updated six-page PDF was built twice; changed pages5/6 rendered/inspected.
Current output SHA256:
`3AB893F19228F766F9C8534546C92CC3487A007346644E778F473E35E5DD9C18`.
